# mcp-deployment

Small deployment helper for MCP (example project). This repository contains a minimal
Python package layout under `src/mcpserver` and project configuration files.

See `pyproject.toml` for project metadata and `main.py` for a simple entrypoint.
# mcp-deployment
